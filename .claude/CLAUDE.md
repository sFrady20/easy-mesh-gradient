do not run bun build after every change, as I am probably running the dev server and it will break both
use bun as the package manager
review all documentation and actively keep it up to date
keep comments concise, use them for readability not explanation
do not "preview" or use a browser, i will test manually and provide feedback if needed
